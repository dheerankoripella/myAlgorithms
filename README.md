# SortAlgorithm

1. Depending on the array size, create a nested for loop
2. Create a temporary variable to store the array value of the current position
3. Check if arr[i] value is greater than arr[i+1] then switch
4. Use the temporary variable to save its array spot and value
5. Display re-ordered array, Least --> Greatest
